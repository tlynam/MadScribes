<article>
  <h1><%= @story.title %></h1>
  <p><%= @story.body %></p>
  <table class="table">
    <body>
      <% @story.sentences.each do |sentence| %>
        <tr>
          <p><%= sentence.body %></p>
        </tr>
      <% end %>
    </body>
  </table>
</article>